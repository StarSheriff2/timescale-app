<%= form_with(model: page_load) do |form| %>
  <% if page_load.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(page_load.errors.count, "error") %> prohibited this page_load from being saved:</h2>

      <ul>
        <% page_load.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_agent, style: "display: block" %>
    <%= form.text_field :user_agent %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
